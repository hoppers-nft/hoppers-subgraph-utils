type BurnedStat @entity {
  id: ID!
  burned: BigInt!
}

type BurnedByBreeding @entity {
  id: ID!
  burned: BigInt!
}

type FlyStaker @entity {
  id: ID!
  staked: BigInt!
}

type VeFly  @entity {
  id: ID!
  flyBalance: BigInt!
  veFlyBalance: BigInt!
  snapshot: Int!
}
